interface:
  display_name: "Incremental Implementation"
  short_description: "Deliver multi-step changes in safe reviewable slices"
  default_prompt: "Use $incremental-implementation to split this non-trivial change into safe verified slices."

policy:
  allow_implicit_invocation: true
