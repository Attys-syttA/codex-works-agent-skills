interface:
  display_name: "Deprecation and Migration"
  short_description: "Plan explicit compatible deprecations and migrations"
  default_prompt: "Use $deprecation-and-migration to plan this compatibility change and its safe rollback."

policy:
  allow_implicit_invocation: false
